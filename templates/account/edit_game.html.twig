{% extends 'base.html.twig' %}

{% block title %}BabyFoot By CCI - Éditer le match{% endblock %}

{% block body %}
<div class="hero overlay" style="background-image: url('/images/account.jpg');">
      <div class="container">
        <div class="row align-items-center">
          <div class="col-lg-12">          
          <div class="d-flex register_card flex-column">
          <div class="p-4">
    <h1>Éditer le match</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}

    </div>
        </div>
        </div>
      </div>
    </div>
{% endblock %}